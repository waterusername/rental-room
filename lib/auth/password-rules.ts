/** Server password rule. Forms and error text must use these values. */
export const MIN_PASSWORD_LENGTH = 10;
export const MAX_PASSWORD_LENGTH = 128;

/** Shown under a field where the person chooses a password. No other composition rules exist. */
export const PASSWORD_HINT = `At least ${MIN_PASSWORD_LENGTH} characters.`;
